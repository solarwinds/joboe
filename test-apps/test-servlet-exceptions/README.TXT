This is a small test application for exceptions tracking on servlet level.

The app is accessible by default at http://<host>/test-servlet-exceptions/

for example:
http://localhost:8080/test-servlet-exceptions/