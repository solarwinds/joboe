This is a simple sample soap server to take soap request and response with x-trace id. 

This is suitable for JDK 11+ as this includes the XML dependency which was removed from JDK 11 distributions onwards

build the war file using maven and then deploy it to any app container. The soap server should be available when the app server is started at port 8888.
  
