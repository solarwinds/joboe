This is a simple sample soap server to take soap request and response with x-trace id. 

build the war file using maven and then deploy it to any app container. The soap server should be available when the app server is started.
  
